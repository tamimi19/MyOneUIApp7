<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- لا تعريف محلي لـ OneUITheme هنا — نستخدم الثيم المزوّد بواسطة oneui-design -->
    <!-- Expanded and collapsed title use One UI attributes so they'll resolve at runtime -->
    <style name="ExpandedTitle" parent="TextAppearance.AppCompat.Title">
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
    </style>

    <style name="CollapsedTitle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
    </style>

</resources>
