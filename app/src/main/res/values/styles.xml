<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- لا تُعرِّف OneUITheme هنا — استخدم الثيم من مكتبة oneui-design في الـ Manifest -->

    <!-- Text appearances with fixed colors (avoid ?attr resolution) -->
    <style name="ExpandedTitleFixed" parent="TextAppearance.AppCompat.Title">
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/oneui_on_primary</item>
    </style>

    <style name="CollapsedTitleFixed" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/oneui_on_primary</item>
    </style>

</resources>
